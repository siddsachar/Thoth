---
title: Settings Controls
description: Generated control-level settings reference with defaults, effects, dependencies, restart notes, and security notes.
---

import GeneratedNotice from '@site/src/components/GeneratedNotice';

# Settings Controls

<!-- generated-from: scripts/docs/collect_inventory.py -->
<!-- inventory-hash: 3bf576cd02047b6d -->

<GeneratedNotice source="scripts/docs/collect_inventory.py" />

## Providers Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Callback URL or authorization code | input |  |  | Changes Callback URL or authorization code in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1029` |
| Cancel | button |  |  | Changes Cancel in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1057` |
| Connect with pasted code | button |  |  | Changes Connect with pasted code in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1058` |
| Close | button |  |  | Changes Close in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1359` |
| Display name | input | &#123;&#125; |  | Changes Display name in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1368` |
| Base URL | input | &#123;&#125; |  | Changes Base URL in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1372` |
| Endpoint id | input | &#123;&#125; |  | Changes Endpoint id in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1377` |
| Profile | input | &#123;&#125; |  | Changes Profile in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1378` |
| Location | input | &#123;&#125; |  | Changes Location in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1379` |
| No API key required | checkbox |  |  | Changes No API key required in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1380` |
| API key or token | input |  |  | Changes API key or token in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1381` |
| Vision input | select | &#123;&#125; |  | Changes Vision input in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1398` |
| Tool calling | select | &#123;&#125; |  | Changes Tool calling in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1404` |
| Native context limit | input | &#123;&#125; |  | Changes Native context limit in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1410` |
| Cancel | button |  |  | Changes Cancel in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1466` |
| Save | button |  |  | Changes Save in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1467` |
| Name | input |  |  | Changes Name in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1560` |
| Base URL | input |  |  | Changes Base URL in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1561` |
| Endpoint profile | select | generic_openai |  | Changes Endpoint profile in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1567` |
| No API key required | checkbox | True |  | Changes No API key required in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1572` |
| Execution location | select | local |  | Changes Execution location in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1573` |
| API key or token | input |  |  | Changes API key or token in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1578` |
| Vision input | select | auto |  | Changes Vision input in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1588` |
| Tool calling | select | auto |  | Changes Tool calling in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1593` |
| Native context limit | input |  |  | Changes Native context limit in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1598` |
| Save Endpoint | button |  |  | Changes Save Endpoint in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:1641` |
| Paste a new value | input |  |  | Changes Paste a new value in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:439` |
| Clear key | button |  |  | Changes Clear key in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:529` |
| Cancel | button |  |  | Changes Cancel in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:733` |
| Check Login | button |  |  | Changes Check Login in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:734` |
| Authorization code | input |  |  | Changes Authorization code in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:782` |
| Cancel | button |  |  | Changes Cancel in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:818` |
| Connect | button |  |  | Changes Connect in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:819` |
| Setup token | input |  |  | Changes Setup token in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:832` |
| Cancel | button |  |  | Changes Cancel in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:860` |
| Import | button |  |  | Changes Import in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:861` |
| OAuth client ID override | input | resolved_client_id |  | Changes OAuth client ID override in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:918` |
| Reset to default | button |  |  | Changes Reset to default in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:950` |
| Cancel | button |  |  | Changes Cancel in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:951` |
| Save override | button |  |  | Changes Save override in Providers settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Credentials are stored through the configured secret store; values are not shown in this reference. | `src/row_bot/ui/provider_settings.py:952` |

## Models Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Default chat model | dynamic | Configured locally | Shown inline | Changes Default chat model in Models settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| Quick Choices | dynamic | Configured locally | Shown inline | Changes Quick Choices in Models settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| Refresh model catalog | dynamic | Configured locally | Shown inline | Changes Refresh model catalog in Models settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| Refresh catalog | button |  |  | Changes Refresh catalog in Models settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:2709` |

## Documents Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Provider | select |  | &#123;'cloud': 'Cloud embedding model', 'local': 'Local runtime model'&#125; | Changes Provider in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1028` |
| Local model | select |  | local_options | Changes Local model in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1033` |
| Cloud model | select |  | cloud_options | Changes Cloud model in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1038` |
| Dimension override | number | &#123;&#125; |  | Changes Dimension override in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1043` |
| Auto-unload local embedding resources after heavy work | switch | &#123;&#125; |  | Changes Auto-unload local embedding resources after heavy work in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1049` |
| Save embedding settings | button |  |  | Changes Save embedding settings in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1183` |
| Rebuild document vectors | button |  |  | Changes Rebuild document vectors in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1184` |
| Rebuild memory index | button |  |  | Changes Rebuild memory index in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1185` |
| Retry local load | button |  |  | Changes Retry local load in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1187` |
| Download model | button |  |  | Changes Download model in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1188` |
| Repair local model | button |  |  | Changes Repair local model in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1193` |
| Resume | button |  |  | Changes Resume in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1284` |
| Pause | button |  |  | Changes Pause in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1293` |
| Cancel remaining | button |  |  | Changes Cancel remaining in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1301` |
| Clear finished | button |  |  | Changes Clear finished in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1366` |
| Clear all documents | button |  |  | Changes Clear all documents in Documents settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:1464` |

## Search Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Web search | dynamic | Configured locally | Shown inline | Changes Web search in Search settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| DuckDuckGo | dynamic | Configured locally | Shown inline | Changes DuckDuckGo in Search settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| Wolfram Alpha | dynamic | Configured locally | Shown inline | Changes Wolfram Alpha in Search settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| arXiv | dynamic | Configured locally | Shown inline | Changes arXiv in Search settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| Wikipedia | dynamic | Configured locally | Shown inline | Changes Wikipedia in Search settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| YouTube | dynamic | Configured locally | Shown inline | Changes YouTube in Search settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| Compression mode | select | &#123;&#125; | _comp_options | Changes Compression mode in Search settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3184` |

## Skills Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Search skills | input |  |  | Changes Search skills in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:2779` |
| Filter | select | All |  | Changes Filter in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:2784` |
| Sort | select | Name |  | Changes Sort in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:2789` |
| Browse Skills | button |  |  | Changes Browse Skills in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:2795` |
| Create Skill | button |  |  | Changes Create Skill in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:2800` |
| Close | button |  |  | Changes Close in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3006` |
| Cancel | button |  |  | Changes Cancel in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3043` |
| Uninstall | button |  |  | Changes Uninstall in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3053` |
| Name (identifier) | input |  |  | Changes Name (identifier) in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3064` |
| Display Name | input |  |  | Changes Display Name in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3072` |
| Icon | select | _icon | _wf_icon_opts | Changes Icon in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3082` |
| Description (one line) | input |  |  | Changes Description (one line) in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3083` |
| Tags (comma-separated) | input |  |  | Changes Tags (comma-separated) in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3087` |
| Cancel | button |  |  | Changes Cancel in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3108` |
| Save | button |  |  | Changes Save in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3138` |
| Cancel | button |  |  | Changes Cancel in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3150` |
| Delete | button |  |  | Changes Delete in Skills settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:3156` |

## System Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Stop | button |  |  | Changes Stop in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:288` |
| Resume | button |  |  | Changes Resume in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:296` |
| Take over | button |  |  | Changes Take over in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:298` |
| Computer Use (Beta) | switch | &#123;&#125; |  | Changes Computer Use (Beta) in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:318` |
| Cancel | button |  |  | Changes Cancel in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:356` |
| Continue | button |  |  | Changes Continue in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:357` |
| Cancel | button |  |  | Changes Cancel in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:457` |
| Remove | button |  |  | Changes Remove in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:468` |
| Reinstall | button |  |  | Changes Reinstall in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:491` |
| Remove | button |  |  | Changes Remove in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:492` |
| Absolute Cua executable path | input |  |  | Changes Absolute Cua executable path in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:503` |
| Use this reviewed system binary instead of Row-Bot's managed runtime | checkbox |  |  | Changes Use this reviewed system binary instead of Row-Bot's managed runtime in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:504` |
| Verify system Cua | button |  |  | Changes Verify system Cua in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:512` |
| Check setup | button |  |  | Changes Check setup in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:546` |
| Recheck | button |  |  | Changes Recheck in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:558` |
| Open Accessibility Settings | button |  |  | Changes Open Accessibility Settings in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:588` |
| Open Screen Recording Settings | button |  |  | Changes Open Screen Recording Settings in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:594` |
| Recheck | button |  |  | Changes Recheck in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/computer_use.py:599` |
| Invite a device | button |  |  | Changes Invite a device in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/remote_access_settings.py:345` |
| Sign out this browser | button |  |  | Changes Sign out this browser in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/remote_access_settings.py:375` |
| Connection route | select |  |  | Changes Connection route in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/remote_access_settings.py:424` |
| Create invitation | button |  |  | Changes Create invitation in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/remote_access_settings.py:510` |
| Copy invitation link | button |  |  | Changes Copy invitation link in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/remote_access_settings.py:553` |
| I reviewed this exact private Serve change. | checkbox |  |  | Changes I reviewed this exact private Serve change. in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/remote_access_settings.py:738` |
| Enable private Serve route | button |  |  | Changes Enable private Serve route in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/remote_access_settings.py:784` |
| Disable Row-Bot route | button |  |  | Changes Disable Row-Bot route in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/remote_access_settings.py:831` |
| Check Tailscale status | button |  |  | Changes Check Tailscale status in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/remote_access_settings.py:837` |
| Review private route | button |  |  | Changes Review private route in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/remote_access_settings.py:842` |
| Allow local-network connections | switch | enabled |  | Changes Allow local-network connections in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/remote_access_settings.py:887` |
| Workspace folder | input |  |  | Changes Workspace folder in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/settings.py:3335` |
| Browse | button |  |  | Changes Browse in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/settings.py:3346` |
| Enable Shell tool | switch | &#123;&#125; |  | Changes Enable Shell tool in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/settings.py:3360` |
| Additional blocked patterns (comma-separated) | input |  |  | Changes Additional blocked patterns (comma-separated) in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/settings.py:3367` |
| Enable Browser tool | switch | &#123;&#125; |  | Changes Enable Browser tool in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/settings.py:3383` |
| Install browser runtime | button |  |  | Changes Install browser runtime in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/settings.py:3403` |
| Enable Filesystem tool | switch | &#123;&#125; |  | Changes Enable Filesystem tool in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/settings.py:3418` |
| File log level | select | &#123;&#125; |  | Changes File log level in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/settings.py:3539` |
| Open Log Folder | button |  |  | Changes Open Log Folder in System settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Filesystem, shell, browser, network, and mobile-access controls can widen local access. | `src/row_bot/ui/settings.py:3556` |

## Accounts Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Reconnect GitHub CLI | dynamic | Configured locally | Shown inline | Changes Reconnect GitHub CLI in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `runtime registry` |
| Refresh GitHub CLI authorisation | dynamic | Configured locally | Shown inline | Changes Refresh GitHub CLI authorisation in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `runtime registry` |
| Use anonymous GitHub access for public sources | dynamic | Configured locally | Shown inline | Changes Use anonymous GitHub access for public sources in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `runtime registry` |
| Clear saved GitHub token | dynamic | Configured locally | Shown inline | Changes Clear saved GitHub token in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `runtime registry` |
| Connect Google account | dynamic | Configured locally | Shown inline | Changes Connect Google account in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `runtime registry` |
| Enable X tool | dynamic | Configured locally | Shown inline | Changes Enable X tool in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `runtime registry` |
| Gmail | switch | &#123;&#125; |  | Changes Gmail in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3597` |
| Calendar | switch | &#123;&#125; |  | Changes Calendar in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3602` |
| Next | button |  |  | Changes Next in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3621` |
| Next | button |  |  | Changes Next in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3629` |
| Back | button |  |  | Changes Back in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3630` |
| Next | button |  |  | Changes Next in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3640` |
| Back | button |  |  | Changes Back in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3641` |
| Next | button |  |  | Changes Next in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3651` |
| Back | button |  |  | Changes Back in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3652` |
| Back | button |  |  | Changes Back in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3659` |
| credentials.json path | input |  |  | Changes credentials.json path in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3666` |
| Browse... | button |  |  | Changes Browse... in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3698` |
| Re-authenticate Google | button |  |  | Changes Re-authenticate Google in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3763` |
| Authenticate Google | button |  |  | Changes Authenticate Google in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:3774` |
| Save Token | button |  |  | Changes Save Token in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4008` |
| Check GitHub | button |  |  | Changes Check GitHub in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4009` |
| Reconnect CLI | button |  |  | Changes Reconnect CLI in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4010` |
| Refresh CLI Auth | button |  |  | Changes Refresh CLI Auth in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4011` |
| Use Anonymous For Public Sources | button |  |  | Changes Use Anonymous For Public Sources in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4012` |
| Clear Saved Token | button |  |  | Changes Clear Saved Token in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4013` |
| Enable X tool | switch | &#123;&#125; |  | Changes Enable X tool in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4060` |
| Next | button |  |  | Changes Next in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4080` |
| Next | button |  |  | Changes Next in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4089` |
| Back | button |  |  | Changes Back in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4090` |
| Next | button |  |  | Changes Next in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4102` |
| Back | button |  |  | Changes Back in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4103` |
| Back | button |  |  | Changes Back in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4113` |
| Save | button |  |  | Changes Save in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4211` |
| Re-authenticate | button |  |  | Changes Re-authenticate in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4214` |
| Authenticate | button |  |  | Changes Authenticate in Accounts settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Connecting an account opens an external authorisation flow. | `src/row_bot/ui/settings.py:4216` |

## Utilities Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Tasks | dynamic | Configured locally | Shown inline | Changes Tasks in Utilities settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| Timer | dynamic | Configured locally | Shown inline | Changes Timer in Utilities settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| URL reader | dynamic | Configured locally | Shown inline | Changes URL reader in Utilities settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| Calculator | dynamic | Configured locally | Shown inline | Changes Calculator in Utilities settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| Weather | dynamic | Configured locally | Shown inline | Changes Weather in Utilities settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| Charts | dynamic | Configured locally | Shown inline | Changes Charts in Utilities settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| System information | dynamic | Configured locally | Shown inline | Changes System information in Utilities settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| Conversation search | dynamic | Configured locally | Shown inline | Changes Conversation search in Utilities settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |
| Custom Tool builder | dynamic | Configured locally | Shown inline | Changes Custom Tool builder in Utilities settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `runtime registry` |

## Tracker Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Enable Habit Tracker | switch | &#123;&#125; |  | Changes Enable Habit Tracker in Tracker settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4333` |
| Delete All Tracker Data | button |  |  | Changes Delete All Tracker Data in Tracker settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4414` |

## Knowledge Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Enable Memory | switch | &#123;&#125; |  | Changes Enable Memory in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4442` |
| Enable Wiki Vault | switch | vault_enabled |  | Changes Enable Wiki Vault in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4492` |
| Browse | button |  |  | Changes Browse in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4503` |
| Apply | button |  |  | Changes Apply in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4511` |
| Sync from Vault | button |  |  | Changes Sync from Vault in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4552` |
| Check vault sync | button |  |  | Changes Check vault sync in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4557` |
| Sync from Vault | button |  |  | Changes Sync from Vault in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4585` |
| Rebuild Vault | button |  |  | Changes Rebuild Vault in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4603` |
| Open Vault Folder | button |  |  | Changes Open Vault Folder in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4623` |
| Filter by category | select | All | cat_options | Changes Filter by category in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4693` |
| Status | select | All | STATUS_OPTIONS | Changes Status in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4695` |
| Source | select | All | SOURCE_OPTIONS | Changes Source in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4696` |
| Tier | select | All | TIER_OPTIONS | Changes Tier in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4697` |
| Search knowledge | input |  |  | Changes Search knowledge in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4698` |
| Select | button |  |  | Changes Select in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4703` |
| Edit | button |  |  | Changes Edit in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4833` |
| Resolve | button |  |  | Changes Resolve in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4834` |
| Archive | button |  |  | Changes Archive in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4839` |
| Delete | button |  |  | Changes Delete in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4920` |
| Edit | button |  |  | Changes Edit in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4925` |
| Restore | button |  |  | Changes Restore in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4928` |
| Archive | button |  |  | Changes Archive in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4934` |
| Resolve | button |  |  | Changes Resolve in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4940` |
| Load more | button |  |  | Changes Load more in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:4951` |
| Delete all knowledge | button |  |  | Changes Delete all knowledge in Knowledge settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5201` |

## Buddy Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Desktop overlay | switch | desktop_initial |  | Changes Desktop overlay in Buddy settings. | Desktop overlay behaviour depends on native-window support. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/buddy.py:1029` |
| Companion personality | select | &#123;&#125; |  | Changes Companion personality in Buddy settings. | Desktop overlay behaviour depends on native-window support. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/buddy.py:1038` |
| Bubble style | select | &#123;&#125; |  | Changes Bubble style in Buddy settings. | Desktop overlay behaviour depends on native-window support. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/buddy.py:1043` |
| Concept | textarea | &#123;&#125; |  | Changes Concept in Buddy settings. | Desktop overlay behaviour depends on native-window support. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/buddy.py:1171` |
| Style notes (optional) | textarea | &#123;&#125; |  | Changes Style notes (optional) in Buddy settings. | Desktop overlay behaviour depends on native-window support. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/buddy.py:1175` |
| Save | button |  |  | Changes Save in Buddy settings. | Desktop overlay behaviour depends on native-window support. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/buddy.py:1390` |
| Use still only | button |  |  | Changes Use still only in Buddy settings. | Desktop overlay behaviour depends on native-window support. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/buddy.py:1391` |
| Retry motion | button |  |  | Changes Retry motion in Buddy settings. | Desktop overlay behaviour depends on native-window support. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/buddy.py:1392` |
| Generate full Buddy | button |  |  | Changes Generate full Buddy in Buddy settings. | Desktop overlay behaviour depends on native-window support. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/buddy.py:1393` |

## Voice Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Talk provider | select | talk_provider_value | talk_provider_options | Changes Talk provider in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5354` |
| Talk model | select | talk_model_value | talk_model_options | Changes Talk model in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5362` |
| Fallback to local Talk if Realtime is unavailable | switch | realtime_fallback_to_local |  | Changes Fallback to local Talk if Realtime is unavailable in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5383` |
| Realtime voice | select | realtime_voice | REALTIME_VOICE_OPTIONS | Changes Realtime voice in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5397` |
| Dictation provider | select | dictation_provider_value | &#123;&#125; | Changes Dictation provider in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5410` |
| Dictation model | select | dictation_model_value | dictation_model_options | Changes Dictation model in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5418` |
| Whisper model size | select | whisper_size | whisper_opts | Changes Whisper model size in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5430` |
| Read-aloud provider | select | speech_output_provider_value | &#123;&#125; | Changes Read-aloud provider in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5444` |
| Read-aloud model | select | speech_output_model_value | speech_model_options | Changes Read-aloud model in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5452` |
| Install Kokoro TTS | button |  |  | Changes Install Kokoro TTS in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5481` |
| Enable text-to-speech | switch | enabled |  | Changes Enable text-to-speech in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5483` |
| Voice | select | voice | voice_opts | Changes Voice in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5488` |
| Auto-speak voice responses | switch | auto_speak |  | Changes Auto-speak voice responses in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5497` |
| Test voice | button |  |  | Changes Test voice in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5503` |
| Open Providers | button |  |  | Changes Open Providers in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5549` |
| Open Providers | button |  |  | Changes Open Providers in Voice settings. | Voice extras, provider credentials, and OS audio permissions may be required. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:5624` |

## Channels Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Discord: Bot Token | dynamic | Configured locally | Shown inline | Changes Discord: Bot Token in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Discord: Your Discord User ID (optional) | dynamic | Configured locally | Shown inline | Changes Discord: Your Discord User ID (optional) in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Discord: Start or stop | dynamic | Configured locally | Shown inline | Changes Discord: Start or stop in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Discord: Test connection | dynamic | Configured locally | Shown inline | Changes Discord: Test connection in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Slack: App Token (xapp-...) | dynamic | Configured locally | Shown inline | Changes Slack: App Token (xapp-...) in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Slack: Bot Token (xoxb-...) | dynamic | Configured locally | Shown inline | Changes Slack: Bot Token (xoxb-...) in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Slack: Your Slack User ID (optional) | dynamic | Configured locally | Shown inline | Changes Slack: Your Slack User ID (optional) in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Slack: Start or stop | dynamic | Configured locally | Shown inline | Changes Slack: Start or stop in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Slack: Test connection | dynamic | Configured locally | Shown inline | Changes Slack: Test connection in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| SMS: Account SID | dynamic | Configured locally | Shown inline | Changes SMS: Account SID in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| SMS: Auth Token | dynamic | Configured locally | Shown inline | Changes SMS: Auth Token in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| SMS: Twilio Phone Number | dynamic | Configured locally | Shown inline | Changes SMS: Twilio Phone Number in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| SMS: Your Phone Number | dynamic | Configured locally | Shown inline | Changes SMS: Your Phone Number in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| SMS: Start or stop | dynamic | Configured locally | Shown inline | Changes SMS: Start or stop in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| SMS: Test connection | dynamic | Configured locally | Shown inline | Changes SMS: Test connection in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Telegram: Bot Token | dynamic | Configured locally | Shown inline | Changes Telegram: Bot Token in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Telegram: Your Telegram User ID | dynamic | Configured locally | Shown inline | Changes Telegram: Your Telegram User ID in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Telegram: Start or stop | dynamic | Configured locally | Shown inline | Changes Telegram: Start or stop in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Telegram: Test connection | dynamic | Configured locally | Shown inline | Changes Telegram: Test connection in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| WhatsApp: Your Phone Number (optional) | dynamic | Configured locally | Shown inline | Changes WhatsApp: Your Phone Number (optional) in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| WhatsApp: Start or stop | dynamic | Configured locally | Shown inline | Changes WhatsApp: Start or stop in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| WhatsApp: Test connection | dynamic | Configured locally | Shown inline | Changes WhatsApp: Test connection in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `runtime registry` |
| Tunnel credentials are in System | button |  |  | Changes Tunnel credentials are in System in Channels settings. | The matching channel extra and third-party account configuration are required. | Follow any inline restart or reconnect prompt shown after changing the value. | Starting an adapter can receive or deliver real messages; review targets first. | `src/row_bot/ui/settings.py:5672` |

## MCP Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Enable MCP | switch | &#123;&#125; |  | Changes Enable MCP in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:222` |
| Add Server | button |  |  | Changes Add Server in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:229` |
| Import Config | button |  |  | Changes Import Config in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:230` |
| Browse MCP Servers | button |  |  | Changes Browse MCP Servers in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:231` |
| Diagnostics | button |  |  | Changes Diagnostics in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:232` |
| Require approval | checkbox | &#123;&#125; |  | Changes Require approval in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:331` |
| Expose resources as utility tools | checkbox | &#123;&#125; |  | Changes Expose resources as utility tools in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:348` |
| Expose prompts as utility tools | checkbox | &#123;&#125; |  | Changes Expose prompts as utility tools in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:353` |
| Name | input |  |  | Changes Name in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:434` |
| Transport | select | &#123;&#125; |  | Changes Transport in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:435` |
| Command | input | &#123;&#125; |  | Changes Command in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:440` |
| Arguments | input | &#123;&#125; |  | Changes Arguments in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:441` |
| URL | input | &#123;&#125; |  | Changes URL in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:442` |
| Environment JSON | textarea | &#123;'indent': 2&#125; |  | Changes Environment JSON in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:443` |
| Headers JSON | textarea | &#123;'indent': 2&#125; |  | Changes Headers JSON in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:444` |
| Timeout seconds | number | &#123;&#125; |  | Changes Timeout seconds in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:446` |
| Output cap characters | number | &#123;&#125; |  | Changes Output cap characters in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:447` |
| Cancel | button |  |  | Changes Cancel in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:484` |
| Save Disabled | button |  |  | Changes Save Disabled in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:485` |
| Test & Save | button |  |  | Changes Test & Save in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:486` |
| JSON | textarea |  |  | Changes JSON in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:494` |
| Cancel | button |  |  | Changes Cancel in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:504` |
| Import Config | button |  |  | Changes Import Config in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:505` |
| Search | input |  |  | Changes Search in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:534` |
| Search Directories | button |  |  | Changes Search Directories in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:536` |
| Close | button |  |  | Changes Close in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:540` |
| Import | button |  |  | Changes Import in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:584` |
| Close | button |  |  | Changes Close in MCP settings. | The MCP extra and a compatible external server are required. | Follow any inline restart or reconnect prompt shown after changing the value. | External servers can expose consequential tools; keep new servers disabled until tested. | `src/row_bot/ui/mcp_settings.py:596` |

## Plugins Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Configure | button |  |  | Changes Configure in Plugins settings. | Plugin-provided dependencies remain disabled until reviewed and installed. | Follow any inline restart or reconnect prompt shown after changing the value. | Review source, permissions, and provided capabilities before enabling. | `src/row_bot/plugins/ui_settings.py:117` |
| Run Test | button |  |  | Changes Run Test in Plugins settings. | Plugin-provided dependencies remain disabled until reviewed and installed. | Follow any inline restart or reconnect prompt shown after changing the value. | Review source, permissions, and provided capabilities before enabling. | `src/row_bot/plugins/ui_settings.py:122` |
| Cancel | button |  |  | Changes Cancel in Plugins settings. | Plugin-provided dependencies remain disabled until reviewed and installed. | Follow any inline restart or reconnect prompt shown after changing the value. | Review source, permissions, and provided capabilities before enabling. | `src/row_bot/plugins/ui_settings.py:205` |
| Uninstall | button |  |  | Changes Uninstall in Plugins settings. | Plugin-provided dependencies remain disabled until reviewed and installed. | Follow any inline restart or reconnect prompt shown after changing the value. | Review source, permissions, and provided capabilities before enabling. | `src/row_bot/plugins/ui_settings.py:221` |
| Browse Marketplace | button |  |  | Changes Browse Marketplace in Plugins settings. | Plugin-provided dependencies remain disabled until reviewed and installed. | Follow any inline restart or reconnect prompt shown after changing the value. | Review source, permissions, and provided capabilities before enabling. | `src/row_bot/plugins/ui_settings.py:32` |
| Reload | button |  |  | Changes Reload in Plugins settings. | Plugin-provided dependencies remain disabled until reviewed and installed. | Follow any inline restart or reconnect prompt shown after changing the value. | Review source, permissions, and provided capabilities before enabling. | `src/row_bot/plugins/ui_settings.py:37` |
| Browse Marketplace | button |  |  | Changes Browse Marketplace in Plugins settings. | Plugin-provided dependencies remain disabled until reviewed and installed. | Follow any inline restart or reconnect prompt shown after changing the value. | Review source, permissions, and provided capabilities before enabling. | `src/row_bot/plugins/ui_settings.py:63` |

## Preferences Controls

| Control | Type | Default | Allowed values | Effect | Dependencies | Restart | Security | Source |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Name | input |  |  | Changes Name in Preferences settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:6023` |
| Personality | textarea |  |  | Changes Personality in Preferences settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:6051` |
| Enable self-improvement | switch | &#123;&#125; |  | Changes Enable self-improvement in Preferences settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:6106` |
| Open Migration Wizard | button |  |  | Changes Open Migration Wizard in Preferences settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/settings.py:6129` |
| Channel | select | channel |  | Changes Channel in Preferences settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/update_dialog.py:173` |
| Check for updates | button |  |  | Changes Check for updates in Preferences settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/update_dialog.py:219` |
| View update | button |  |  | Changes View update in Preferences settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/update_dialog.py:228` |
| Clear skipped | button |  |  | Changes Clear skipped in Preferences settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/update_dialog.py:241` |
| Skip this version | button |  |  | Changes Skip this version in Preferences settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/update_dialog.py:72` |
| Remind me later | button |  |  | Changes Remind me later in Preferences settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/update_dialog.py:73` |
| Install now | button |  |  | Changes Install now in Preferences settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/update_dialog.py:74` |
| Cancel | button |  |  | Changes Cancel in Preferences settings. | No optional dependency is indicated by the control itself. | Follow any inline restart or reconnect prompt shown after changing the value. | Stored locally unless the surrounding feature explicitly uses an external service. | `src/row_bot/ui/update_dialog.py:91` |
